export default function DashboardPage() {
  return (
    <div style={{ padding: '4vw 6vw', fontFamily: 'var(--font-space-grotesk), sans-serif' }}>
      <h1 style={{ fontSize: '2rem', fontWeight: 700 }}>ANVIL Dashboard</h1>
      <p style={{ color: 'var(--muted)', marginTop: '12px' }}>Coming in the next steps — chat, tool registry, approval queue, and trace replay.</p>
    </div>
  );
}